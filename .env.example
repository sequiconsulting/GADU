# Registry Encryption (Production only - Netlify)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
REGISTRY_ENCRYPTION_KEY="generate_your_64_char_hex_key_here"

# Backend (Netlify Functions)
REGISTRY_ENCRYPTION_KEY="generate-32-char-random-key-here"
